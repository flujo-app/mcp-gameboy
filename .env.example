# Optional cartridge directory and startup file.
ROM_DIR=./roms
# ROM_PATH=./roms/your-homebrew.gb

# Only used with --http, --sse or --ui.
SERVER_HOST=127.0.0.1
SERVER_PORT=3001
# Generate: node -e "console.log(require('node:crypto').randomBytes(32).toString('base64url'))"
# MCP_OPERATOR_TOKEN=
# MCP_PUBLIC_URL=https://gameboy.example.com
# MCP_ALLOWED_ORIGINS=https://trusted-client.example.com
